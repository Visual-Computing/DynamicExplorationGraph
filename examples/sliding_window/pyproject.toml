[project]
name = "sliding-window-benchmark"
version = "0.1.0"
description = "DEG Sliding Window Benchmark (RedCaps experiment from CleANN paper)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "deglib",
    "numpy>=1.24.0",
    "matplotlib>=3.7.0",
    "tqdm>=4.65.0",
    "h5py>=3.8.0",
    "requests>=2.28.0",
]

[tool.uv.sources]
deglib = { path = "../../python", editable = true }
